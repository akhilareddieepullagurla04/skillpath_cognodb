# Leave unset for local dev (uses the Vite proxy to localhost:4000).
# For a deployed build, set this to your deployed backend's URL, e.g:
# VITE_API_URL=https://skillpath-api.onrender.com
VITE_API_URL=
